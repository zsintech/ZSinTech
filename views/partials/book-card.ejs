<article class="book-card">
  <div class="book-card-cover media-cover <%= book.coverUrl ? '' : 'media-cover--placeholder' %>"
       <% if (book.coverUrl) { %>style="background-image: url('<%= typeof img !== 'undefined' ? img(book.coverUrl, 'card') : book.coverUrl %>')"<% } %>>
  </div>
  <div class="book-card-body">
    <span class="book-card-status"><%= book.status === 'have-read' ? 'Have read' : 'To read' %></span>
    <% if (book.category) { %><span class="book-card-category-tag"><%= book.category %></span><% } %>
    <h3><%= book.title %></h3>
    <% if (book.author) { %><p class="book-card-author"><%= book.author %></p><% } %>
    <% if (book.notes) { %><p class="book-card-notes"><%= book.notes %></p><% } %>
    <% if (book.rating) { %><span class="rating">★ <%= book.rating %>/5</span><% } %>
    <% if (book.tags && book.tags.length) { %>
    <div class="tags">
      <% book.tags.slice(0, 4).forEach(tag => { %><span class="tag"><%= tag %></span><% }) %>
    </div>
    <% } %>
  </div>
</article>
